# 사용자 작업 환경 지침

## 기본 원칙

- OneDrive 경로를 Codex 또는 개발 작업 폴더로 사용하지 않는다.
- 사용자가 명시하지 않는 한 `C:\Users\bests\OneDrive\Documents\ChatGPT` 아래에 새 파일이나 폴더를 만들지 않는다.
- 기본 로컬 Codex 작업 루트는 `C:\Users\bests\codex`로 본다.
- 공유 작업공간은 `E:\dev\codex`를 기준으로 한다.
- Codex 앱 자체는 각 기기의 C 드라이브 기본 설치 위치에 둔다.

## 작업 위치 선택

- 새 프로젝트나 문서를 만들 때는 우선 `C:\Users\bests\codex` 아래에 둔다.
- PC와 노트북이 번갈아 사용할 실제 공유 프로젝트는 `E:\dev\codex` 아래에 둔다.
- OneDrive가 자동으로 관리하는 문서/데스크톱 경로는 피한다.

## 파일 작업 전 확인

- 파일을 만들거나 옮기기 전에 대상 경로가 OneDrive 아래인지 확인한다.
- 대상 경로가 `C:\Users\bests\OneDrive`로 시작하면 사용자에게 먼저 확인한다.
- 대상 경로가 `C:\Users\bests\codex` 또는 `E:\dev\codex`이면 사용자의 선호 경로로 간주한다.

## 사용자 선호

- 사용자는 OneDrive가 로컬 폴더를 임의로 관리하거나 웹에 업로드하는 것을 원하지 않는다.
- 사용자는 로컬 폴더와 파일의 위치를 직접 통제하기를 원한다.
- 작업 환경 설명은 PC, 노트북, 외장/공유 E 드라이브를 구분해서 안내한다.
